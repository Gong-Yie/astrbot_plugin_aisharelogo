{
  "font_size": {
    "description": "自定义字体大小。若为0则根据图片宽度自动计算",
    "type": "int",
    "default": 200
  },
  "default_font": {
    "description": "默认使用的字体文件名（必须存在于插件的 font 目录下，带后缀）",
    "type": "string",
    "default": "优设标题黑.ttf"
  },
  "default_logo": {
    "description": "默认使用的Logo样式底图（必须存在于插件的 logo 目录下，带后缀）",
    "type": "string",
    "default": "style1_bg.png"
  },
  "style_type": {
    "description": "文字特效样式（1为深红立体阴影，2为烫金纹理）",
    "type": "int",
    "options": [1, 2],
    "default": 1
  },
  "use_avatar_if_no_image": {
    "description": "如果没有引用图片，则使用该用户的头像作为图片",
    "type": "bool",
    "default": true
  }
}